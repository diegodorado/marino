/ Main navigation
%nav.main-navigation
  / Responsive navbar button
  .navbar
    %a.btn.btn-alt.btn-large.btn-primary.btn-navbar{"data-target" => ".nav-collapse", "data-toggle" => "collapse"}
      %span.icon-home
      Dashboard
  / /Responsive navbar button
  / Navigation
  .nav-collapse.collapse{:role => "navigation"}
    %ul
      %li.active-
        %a{:href => "/"}
          %span.icon-home
          Inicio
      -if can? :manage, Store
        %li
          = link_to stores_path do
            Depositos
      -if can? :access, Backup
        %li
          %a{:href => "/backups"}
            %span.icon-file
            Backups
      -if can? :manage, Crop
        %li
          = link_to admin_crops_path do
            Granos
      -if can? :manage, User      
        %li
          = link_to admin_users_path do
            Usuarios
      -if can? :manage, Company      
        %li
          = link_to admin_companies_path do
            Empresas
