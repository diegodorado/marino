- unless user_signed_in?
  .dark.data-block
    %header
      %h2
        Intranet
    %section
      %p.lead Bienvenido
      %p 
        Para acceder a cualquier sistema de la Intranet es necesario registrarse.
      %p     
        Si ya estás registrado, ingresa 
        = link_to('ingresa aquí', new_user_session_path)
      %p
        De lo contrario, 
        = link_to('registrate', new_user_registration_path)
- else
  .row-fluid
    .span8
      .dark.data-block
        %header
          %h2
            Mis Empresas
      .row-fluid
        - @companies.each do |company|
          %a.span2.data-block.widget-block(href="#{select_company_path(company)}")
            %section
              %span.icon-website-alt
              %strong #{company.name}      
          
    .span4
      .dark.data-block
        %header
          %h2
            Modulos disponibles
        %section          
          -if can? :manage, CropControl
            = link_to summary_crop_controls_path, :class => "btn btn-primary btn-large" do
              Ingresar al Control de Granos          
          -else
            %p Debes seleccionar una empresa para comenzar con el Control de Granos
            
          %br
          %br
          
          -if can? :manage, Backup
            = link_to backups_path, :class => "btn btn-primary btn-large" do
              Ingresar a Backups
          -else
            %p Debes seleccionar una empresa para utilizar Backups
          
